import { NodeSet } from './node-set.js';
import { Primitive } from './primitive.js';
/******************************************************************************/
export function fn(functionName, ...args) {
    const expression = `${functionName}(${args
        .filter((arg) => arg !== undefined)
        .map((arg) => (Primitive.isLiteral(arg) ? Primitive.literal(arg) : arg).expression)
        .join(`, `)})`;
    return functionName === `id`
        ? new NodeSet(expression)
        : new Primitive(expression);
}
//# sourceMappingURL=fn.js.map